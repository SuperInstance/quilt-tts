[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "quilt-tts"
version = "0.1.0"
description = "Canon to speech"
readme = "README.md"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["quilt_tts"]
